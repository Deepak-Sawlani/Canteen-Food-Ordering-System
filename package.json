{
  "name": "mern-app",
  "version": "1.0.0",
  "scripts": {
    "install-client": "cd client && npm install",
    "build-client": "cd client && npm run build",
    "install-all": "npm install && npm run install-client",
    "vercel-build": "npm run install-all && npm run build-client"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "cors": "^2.8.5"
  }
}